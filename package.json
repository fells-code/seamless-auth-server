{
  "name": "seamless-auth-server",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "build": "pnpm -r --sort build",
    "clean": "pnpm -r --sort clean",
    "test": "pnpm -r --sort test",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release:stable": "pnpm build && changeset publish",
    "release:alpha": "changeset version --snapshot alpha && pnpm build && changeset publish --tag alpha --no-git-tag",
    "release:beta": "changeset version --snapshot beta && pnpm build && changeset publish --tag beta --no-git-tag",
    "release:rc": "changeset version --snapshot rc && pnpm build && changeset publish --tag rc --no-git-tag"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.7"
  },
  "pnpm": {
    "overrides": {
      "@babel/core": "^7.29.7",
      "body-parser": "^2.3.0",
      "brace-expansion@1": "^1.1.17",
      "esbuild": "^0.28.1",
      "form-data": "^4.0.6",
      "js-yaml@3": "^3.15.0",
      "js-yaml@4": "^4.3.0"
    }
  }
}
